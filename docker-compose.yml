version: '3.9'

services:
  ros_core:
    build:
      context: .
    command: ros2 run core core
    restart: always

  ros_robot:
    build:
      context: .
    command: ros2 run robot node
    restart: always

#  ros_cmd:
#    build:
#      context: .
#    command: ros2 run control interface
#    restart: always

#  redis:
#    image: redis:latest
#    restart: always
#    ports:
#      - '6379:6379'
#    command: redis-server
#    volumes:
#      - cache:/data
#
#volumes:
#  cache:
#    driver: local
